<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }}</title>
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, minimum-scale=1.0" />
    {% include "../head-js-css.njk" %}
  </head>
  <body class="primary">
    <header role="banner" id="header" class="global-header fixed">
      <div id="skip-to-content">
        <a href="#main-content">Skip to Main Content</a>
      </div>
      {% include "../utility-header.njk" %} 
      {% include "../site-header-icons.njk" %} 
      {% include "../mobile-controls.njk" %}
      <div class="navigation-search">
        <!-- navigation types -->
        {%- if navigation -%} {%- include navigation -%} {%- else -%} {%-
        include "../navigation.njk" -%} {%- endif %}
        <div
          id="head-search"
          class="search-container {{- ' featured-search' if search === 'Featured' }}">
          {% include "../search.njk" %}
        </div>
      </div>
    </header>

    {% block content %}
    <div id="main-content" class="main-content">
      <main class="main-primary" id="main">{{ content | safe }}</main>
    </div>
    {% endblock %} {% include "../global-footer.njk" %}
  </body>
</html>